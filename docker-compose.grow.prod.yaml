services:
  web:
    build:
      context: .
      dockerfile: ./docker/common/Dockerfile
      target: web
    restart: unless-stopped
    volumes:
      - grow-laravel-storage-production:/var/www/storage:ro
      - ./docker/common/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      # Use assets baked into the image; do not override with host build dir
      # - ./public/build:/var/www/public/build:ro
      - ./public/images:/var/www/public/images:ro
    networks:
      - grow-laravel-production
    ports:
      # NEU: eigener Port für grow.linn.games (Axia nutzt 6478, getaxia nutzt 6479)
      - "6480:80"
    depends_on:
      - php-fpm

  php-fpm:
    build:
      context: .
      dockerfile: ./docker/common/Dockerfile
      target: php-fpm
    restart: unless-stopped
    volumes:
      - grow-laravel-storage-production:/var/www/storage
      # Use assets baked into the image; do not override with host build dir
      # - ./public/build:/var/www/public/build:ro
      - ./public/images:/var/www/public/images:ro
    env_file:
      - .env
    networks:
      - grow-laravel-production
    depends_on:
      postgres:
        condition: service_healthy

  php-cli:
    build:
      context: .
      dockerfile: ./docker/common/php-fpm/dockerfile
    tty: true
    stdin_open: true
    env_file:
      - .env
    networks:
      - grow-laravel-production

  postgres:
    image: postgres:16
    restart: unless-stopped
    user: postgres
    ports:
      # NEU: anderer Host-Port, damit er sich nicht mit Axia (5433) beißt
      - "5434:5432"
    environment:
      - POSTGRES_DB=${POSTGRES_DATABASE}
      - POSTGRES_USER=${POSTGRES_USERNAME}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - grow-postgres-data-production:/var/lib/postgresql/data
    networks:
      - grow-laravel-production
    healthcheck:
      test: ["CMD", "pg_isready"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:alpine
    restart: unless-stopped
    volumes:
      - grow-redis-data-production:/data
    networks:
      - grow-laravel-production
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

networks:
  grow-laravel-production:
    driver: bridge

volumes:
  grow-postgres-data-production:
  grow-laravel-storage-production:
  grow-redis-data-production:
